id: rule.network.connection_refused.absent
hypothesis: hypothesis.network.connection_refused
when:
  observation: observation.network.connection_refused
  operator: absent
effect:
  confidence: decrease
explanation: If connection establishment fails without an explicit refusal signal, TCP connection refusal is a weaker explanation and timeout, reset, DNS, TLS, admission, authentication, or availability failures should remain candidates.
