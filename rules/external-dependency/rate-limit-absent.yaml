id: rule.external_dependency.rate_limiting.absent
hypothesis: hypothesis.external_dependency.rate_limiting
when:
  observation: observation.dependency.http_rate_limit_response
  operator: absent
effect:
  confidence: decrease
explanation: If the dependency request fails without an explicit throttling response or equivalent rate-limit evidence, reduce confidence in rate limiting and investigate other dependency, protocol, or transport failure mechanisms.
