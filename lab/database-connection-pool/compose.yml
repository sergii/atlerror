services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: atlerror
      POSTGRES_PASSWORD: atlerror
      POSTGRES_DB: atlerror
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U atlerror -d atlerror"]
      interval: 1s
      timeout: 1s
      retries: 30

  app:
    build: .
    image: atlerror-database-connection-pool-lab
    command: ["python", "/lab/app.py"]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: atlerror
      DB_PASSWORD: atlerror
      DB_NAME: atlerror
      APP_PORT: "8080"
      POOL_SIZE: "1"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8080/health', timeout=2).read()"]
      interval: 1s
      timeout: 3s
      retries: 30
    depends_on:
      db:
        condition: service_healthy

  probe:
    image: atlerror-database-connection-pool-lab
    command: ["python", "/lab/probe.py"]
    environment:
      APP_HOST: app
      APP_PORT: "8080"
      HOLD_MS: "300"
    depends_on:
      app:
        condition: service_healthy
