services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: atlerror
      POSTGRES_PASSWORD: atlerror
      POSTGRES_DB: atlerror
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U atlerror -d atlerror"]
      interval: 1s
      timeout: 1s
      retries: 30

  app:
    build: .
    command: ["python", "/lab/app.py"]
    environment:
      DB_HOST: db
      DB_PORT: "5432"
      DB_USER: atlerror
      DB_PASSWORD: atlerror
      DB_NAME: atlerror
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import socket; s=socket.create_connection(('127.0.0.1',8080),1); s.close()"]
      interval: 1s
      timeout: 1s
      retries: 30

  probe:
    build: .
    command: ["python", "/lab/probe.py"]
    environment:
      APP_HOST: app
      APP_PORT: "8080"
    depends_on:
      app:
        condition: service_healthy
